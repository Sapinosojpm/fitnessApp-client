// src/components/Workouts.js

import React from 'react';
import WorkoutList from '../components/WorkoutList'; // Import the WorkoutList component

const Workouts = () => {
  return (
    <div>
      <WorkoutList /> {/* Render the WorkoutList component */}
    </div>
  );
};

export default Workouts;  // Default export for Workouts
